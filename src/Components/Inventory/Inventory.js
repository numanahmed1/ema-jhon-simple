import React from "react";

const Inventory = () => {
  document.title = "Inventory Page";

  return (
    <div>
      <h1>This is for managing Inventory.</h1>
    </div>
  );
};

export default Inventory;
